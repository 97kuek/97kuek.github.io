---
title: "動体検知の基本理論と実装"
description: "背景差分法を用いた動体検知の基本的な理論とOpenCVによる実装手法について解説します。"
publishDate: "2026-12-09"
image: "@assets/blog/motion-detection-app/OpenCV.jpg"
tags: ["Python", "OpenCV", "動体検知", "画像処理"]
---

最近、画像処理について勉強してみようと思い、以下の書籍を手に取りました。

![OpenCV](@assets/blog/motion-detection-app/OpenCV.jpg)

『OpenCVによる画像処理入門』という一冊です。
基礎から応用まで幅広くカバーされており、読み進めるうちに「学んだ理論を実際に動くシステムに反映させたい」という思いが強まりました。そこで今回は、自動ドアや防犯カメラなど、身近なシーンで活用されている**動体検知システム**を、画像処理の古典的な手法である**背景差分法**を用いて実装してみることにしました。

現在、物体検知の分野ではYOLOなどの深層学習（Deep Learning）モデルを用いるのが主流ですが、あえて画像処理の基礎アルゴリズムに立ち返ることで、画素レベルでの処理の本質を理解することを目指します。
なお、作成したソースコードは[こちらのGitHubリポジトリ](https://github.com/97kuek/Motion-Detection-App)で公開しています。

![検出結果](@assets/projects/motion-detection-app/検出結果.gif)

## 動体検知の基本コンセプト

動体検知（Motion Detection）とは、連続する画像（映像）の中から**変化した領域を特定する技術**です。
防犯カメラの不審者検知、自動ドアの開閉制御、交通流の自動計測など、実世界の空間情報をデジタル処理する多くのシステムにおいて、入り口となる重要な役割を担っています。

動体を検知するための最もシンプルかつ直感的な考え方は、**過去の映像の状態と比較して、現在の映像に生じた差分（変化）を見つけ出す**というものです。この考え方をアルゴリズムとして体系化したのが、今回の主役である**背景差分法**です。

## 背景差分法の原理と現実的な課題

背景差分法は、文字通り**現在のフレームから「物体が存在しない状態の背景」を引き算する**ことで、変化した物体（前景）のみを抽出する手法です。

原理自体は極めて明快ですが、実環境に適用しようとすると、以下のような特有の課題に直面します。
- **照明環境の変化**: 太陽光の移ろいや雲の通過による明暗の変化を「動き」と誤認してしまう。
- **背景の揺らぎ**: 風に揺れる木の葉や水面の煌めきなど、本質的でない小さな動きを拾ってしまう。
- **静止物の変化**: 長時間置かれた荷物が背景の一部化するような、背景自体の経時変化への対応。

これらの外乱（ノイズ）に対して頑健（ロバスト）な検知を実現するため、一連の処理パイプラインを構築する必要があります。

### 処理のパイプライン

動体検知プログラムは、カメラから入力される各フレームに対して、以下のステップを順次実行します。

1. **映像の入力と前処理**: 輝度情報に基づく処理を容易にするため、グレースケール化とノイズ低減（平滑化）を行う。
2. **背景モデルの構築**: 時間的な平均化を行い、環境変化に適応し続ける「基準」を作成する。
3. **差分の計算**: 入力フレームと背景モデルの差（絶対値差分）を求める。
4. **適応的2値化（閾値処理）**: 変化量を「0（背景）」か「255（動体）」の2段階に分類し、意味のある候補を浮かび上がらせる。
5. **形態学的処理（モルフォロジー演算）**: ノイズ除去と輪郭の結合を行い、マスク画像を整形する。
6. **輪郭抽出**: 整形された領域を数学的な座標データ（図形の形）として取り出す。
7. **フィルタリング**: 面積などの閾値に基づき、意味のあるサイズの物体のみを有効な動体として認定する。
8. **結果の可視化**: 検知した領域をバウンディングボックスで囲み、元の映像にオーバーレイ表示する。

それでは、各ステップの技術的な詳細を追っていきましょう。

## 1. 映像の入力と前処理

まず、OpenCVを使ってカメラや動画ファイルからフレームを読み込みます。情報の純度を高め、計算コストを最小限に抑えるため、以下の処理を施します。

![グレースケール](@assets/blog/motion-detection-app/grayscale.png)

- **グレースケール化**
  カラー画像（RGB）から輝度情報のみを抽出します。3チャンネルの情報を1チャンネルに絞り込むことで、後続の演算負荷を1/3程度まで軽量化できます。
- **ガウシアンブラーによる平滑化**
  カメラセンサー特有のザラつき（高周波ノイズ）を抑えるために、ガウス分布に基づくぼかし処理を適用します。周辺ピクセルと値を馴染ませることで、微小な点ノイズによる誤検知を大幅に低減できます。

## 2. 背景モデルの動的構築

背景差分法の心臓部は、「背景をいかに定義・維持するか」にあります。単に「最初の1枚」を背景として固定すると、雲の影や照明の変化などの環境変化に全く対応できなくなり、すぐに画面全体が「動き」として誤判定されてしまいます。

そこで、**移動加重平均** を用いて背景モデルを更新し続けるアプローチを採用します。

$$ B_t = (1 - \alpha) B_{t-1} + \alpha F_t $$

- **$B_t$** : 時刻 $t$ における背景モデル
- **$\alpha$** : 学習率
- **$F_t$** : 現在の入力フレーム

この数式は、**「新しい情報を少しずつ背景へ混ぜ込んでいく」**ことを意味します。学習率 $\alpha$ を適切に設定することで、数秒から数分のスパンでの照明変化には対応しつつ、素早く動く物体（検知対象）は背景に取り込まずに抽出することが可能になります。

## 3. 差分の計算（フレーム差分）

動的な背景モデルが完成したら、現在のフレームとの間でピクセルごとの差を求め、その絶対値を生成します。

![背景差分法](@assets/blog/motion-detection-app/background_diff.png)

$$ D_t = |F_t - B_t| $$

ここで白く浮かび上がる領域は、背景モデルには存在しない「変化したピクセル（前景）」の候補です。

## 4. 2値化（閾値処理）と大津の手法

得られた差分画像は濃淡を持つため、どこからが「実際の動き」かを明確にする必要があります。これが2値化処理です。

単一の固定閾値では照明環境の変化に弱いため、今回は**大津の2値化** を活用しました。これは、画像全体のヒストグラムを解析し、背景と前景のクラス間分散が最大となるような「最適な閾値」を自動的に計算するアルゴリズムです。これにより、ユーザーが手動でパラメーターを調整することなく、環境に合わせて最適な「白黒の境界線」を決定できるようになります。

![ohtsu](@assets/projects/motion-detection-app/ohtsu.png)

## 5. モルフォロジー演算（形態学的画像処理）

2値化直後の画像には「ゴマ塩状のノイズ」や「物体の欠損」が含まれることが多いため、これを整形します。

- **オープニング処理:** 小さなノイズを消し去るために「収縮（削る）」した後に「膨張（戻す）」を行います。
- **クロージング処理:** 物体内部の穴を埋めるために「膨張」した後に「収縮」を行います。

これらを適切に組み合わせることで、ノイズが少なく物体の輪郭がはっきりとした「きれいなマスク画像」を得ることができます。

![オープニング](@assets/blog/motion-detection-app/opening.png)

## 6. 輪郭抽出と図形データの取得

整形されたマスク画像から、各ピクセルの塊を「リスト形式の座標データ」に変換します。これが**輪郭抽出** です。

OpenCVの `cv2.findContours` 関数を用い、`cv2.RETR_EXTERNAL`（最も外側の境界のみを追跡）や `cv2.CHAIN_APPROX_SIMPLE`（直線の冗長な点を省略）といったフラグを適切に設定することで、高速かつ正確に物体の形状データを取得できます。この時点で、画像は単なる「点」の集まりから、数学的な「図形のオブジェクト」へと昇華されます。

抽出された輪郭の情報は、次のステップでのフィルタリングや、画面上への可視化に利用されます。

## 7. 面積フィルタリングと事象判定

抽出したすべての輪郭に対して、面積（包含ピクセル数）を計算します。
「面積が1,000ピクセル以下のものは、カメラの揺れや小さな虫などのノイズとみなして無視する」といった**フィルタリングロジック**を導入することで、人間や車などの明確な「動体」のみをフィルタリングして取り出すことができます。

## 8. 結果の可視化とUIへの反映

最終的なステップとして、検知した物体の周囲にバウンディングボックス（矩形）を描画します。画像データ `cv2.rectangle` を用いて、元のカラー映像に情報のレイヤーを重ねることで、システムが何を見ているのかを直感的にユーザーへ伝えます。

## 実装を振り返って

本記事では、古典的なアルゴリズムである背景差分法を用いた動体検知の全工程を、解説してみました。

実用上の限界として、今回紹介した手法は「固定カメラ」であることが前提条件となります。パン・チルトを行うカメラやドローン映像などの場合には、より高度なグローバルモーション補正やAIベースの検知モデルが必要になります。

画像処理の世界は、数学とプログラミングが視覚的な結果として結びつく非常に面白い分野だと思いました。今後もさらに高度なテーマに挑戦していきたいと思います。

さらに詳しく学習したい方は、公式ドキュメントも非常に参考になります。

{% LinkCard url="https://opencv.org/" / %}
