---
interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<header class="content-heading">
  <h1>{title}</h1>
  <p>{description}</p>
  <slot />
</header>
