---
interface Props {
  skill: string;
  href?: string;
}

const { skill, href } = Astro.props;
---

{
  href ? (
    <a
      href={href}
      class="skill-chip skill-chip--link"
    >
      {skill}
    </a>
  ) : (
    <span class="skill-chip">{skill}</span>
  )
}
