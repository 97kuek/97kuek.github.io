---
import { CircleCheck, CircleX, Info, TriangleAlert } from "@lucide/astro";

const { color, title } = Astro.props;

type BoxConfig = {
  wrapper: string;
  iconColor: string;
  icon: any;
};

const configs: Record<string, BoxConfig> = {
  info: {
    wrapper: 'border-info/70 bg-info/5',
    iconColor: 'text-info',
    icon: Info,
  },
  warning: {
    wrapper: 'border-warning/70 bg-warning/10',
    iconColor: 'text-warning',
    icon: TriangleAlert,
  },
  success: {
    wrapper: 'border-success/70 bg-success/5',
    iconColor: 'text-success',
    icon: CircleCheck,
  },
  error: {
    wrapper: 'border-error/70 bg-error/5',
    iconColor: 'text-error',
    icon: CircleX,
  },
};

const cfg = configs[color] ?? {
  wrapper: 'border-base-content/30 bg-base-100',
  iconColor: 'text-base-content/70',
  icon: Info,
};
const IconComponent = cfg.icon;
---

<div class={`my-6 rounded-lg border px-5 py-4 ${cfg.wrapper}`}>
  {title && (
    <div class="not-prose flex items-center gap-1.5 mb-2 text-base-content">
      <IconComponent class={`size-3.5 stroke-[2.5] ${cfg.iconColor}`} aria-hidden="true" />
      <span class="text-sm font-semibold">{title}</span>
    </div>
  )}
  <slot />
</div>
