---
import { renderToString } from 'katex';

const formula = await Astro.slots.render("default");
const html = renderToString(formula.trim(), {
  displayMode: false,
  throwOnError: false
});
---

<span class="katex-inline" set:html={html} />
